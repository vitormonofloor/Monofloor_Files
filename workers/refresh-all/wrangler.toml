name = "monofloor-refresh"
main = "src/index.js"
compatibility_date = "2026-01-01"

# GH_TOKEN é setado via `wrangler secret put GH_TOKEN` ou pelo painel Cloudflare
# (Settings → Variables and Secrets → Add → Encrypted).
# Escopo necessário do PAT: `workflow` (Repo → Workflow). Nada mais.
